# qemu-ubuntu
A ready to use qemu setup for ubuntu

# single vm
To launch a single qemu instance, proceed with `01_single_vm_w_internet`.

# multi vm
To launch multiple qemu instances communicating with each other, proceed with `02_multi_vm_wo_internet`.
