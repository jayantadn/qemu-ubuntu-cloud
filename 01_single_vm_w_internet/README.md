# ubuntu-qemu
A ready-to use qemu image for ubuntu-cloud

# How to use
1. if `noble-server-cloudimg-amd64.img` is not present, execute `join_image.sh`
1. launch vm using `launch.sh`
1. default credentials `username: ubuntu, password: ubuntu` 
